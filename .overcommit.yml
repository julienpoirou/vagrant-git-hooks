PreCommit:
  RuboCop:
    enabled: true
    required_executable: 'bundle'
    command: ['bundle', 'exec', 'rubocop', '-A']
    on_warn: fail

CommitMsg:
  MessageFormat:
    enabled: true
    pattern: '^(revert: )?(feat|fix|perf|refactor|docs|style|test|build|chore|ci)(\([\w\-.,]+\))?(!)?: .+'
    message: 'Use Conventional Commits, ex: feat(installer): back up foreign hooks before replacing'
